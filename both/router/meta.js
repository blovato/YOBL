if(Meteor.isClient) {
  Meta.config({
      options: {
        // Meteor.settings[Meteor.settings.environment].public.meta.title
        title: 'Year of Biblical Literacy',
        suffix: 'Biblical Literacy'
      }
  });
}
